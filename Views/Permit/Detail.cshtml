@model PerizinanPeternakan.ViewModels.PermitDetailViewModel
@{
    ViewData["Title"] = "Detail Permohonan";
    var userRole = Context.Session.GetString("Role");
}

<div class="permit-detail-container">
    <div class="detail-header">
        <div class="header-content">
            <h1 class="detail-title">
                <i class="fas fa-file-alt"></i>
                Detail Permohonan
            </h1>
            <div class="header-actions">
                <a href="@Url.Action("Index", "Permit")" class="btn btn-outline-secondary">
                    <i class="fas fa-arrow-left"></i>
                    Kembali
                </a>

                @if (Model.CanDownload && Context.Session.GetString("Role") == "User")
                {
                    <a href="@Url.Action("Download", "Permit", new { id = Model.Id })" class="btn btn-success">
                        <i class="fas fa-download"></i>
                        Download PDF
                    </a>
                }

                @if (!string.IsNullOrEmpty(Model.GeneratedDocumentPath) &&
                    (Context.Session.GetString("Role") == "Verifikator" || Context.Session.GetString("Role") == "KepalaDinas"))
                {
                    <a href="@Url.Action("ViewDocument", "Permit", new { id = Model.Id })" class="btn btn-info">
                        <i class="fas fa-eye"></i>
                        Lihat Dokumen PDF
                    </a>
                }

                @if (Model.CanApprove)
                {
                    <a href="@Url.Action("Approve", "Permit", new { id = Model.Id })" class="btn btn-primary">
                        <i class="fas fa-gavel"></i>
                        Proses Permohonan
                    </a>
                }
            </div>
        </div>

        <div class="permit-status">
            <span class="status-badge badge-@PermitStatusHelper.GetStatusClass(Model.Status)">
                @PermitStatusHelper.GetStatusText(Model.Status)
            </span>
        </div>
    </div>

    <div class="detail-content">
        <div class="content-grid">
            <!-- Informasi Umum -->
            <div class="info-card">
                <div class="card-header">
                    <h3>
                        <i class="fas fa-info-circle"></i>
                        Informasi Umum
                    </h3>
                </div>
                <div class="card-body">
                    <div class="info-grid">
                        <div class="info-item">
                            <label>Nomor Permohonan</label>
                            <span class="value highlight">@Model.ApplicationNumber</span>
                        </div>
                        <div class="info-item">
                            <label>Tanggal Pengajuan</label>
                            <span class="value">@Model.SubmissionDate.ToString("dd MMMM yyyy")</span>
                        </div>
                        <div class="info-item">
                            <label>Status</label>
                            <span class="status-badge badge-@PermitStatusHelper.GetStatusClass(Model.Status)">
                                @PermitStatusHelper.GetStatusText(Model.Status)
                            </span>
                        </div>
                        @if (Model.ValidFrom.HasValue && Model.ValidUntil.HasValue)
                        {
                            <div class="info-item">
                                <label>Masa Berlaku</label>
                                <span class="value">@Model.ValidFrom.Value.ToString("dd MMM yyyy") - @Model.ValidUntil.Value.ToString("dd MMM yyyy")</span>
                            </div>
                        }
                    </div>
                </div>
            </div>

            <!-- Informasi Pemohon -->
            <div class="info-card">
                <div class="card-header">
                    <h3>
                        <i class="fas fa-user"></i>
                        Informasi Pemohon
                    </h3>
                </div>
                <div class="card-body">
                    <div class="info-grid">
                        <div class="info-item">
                            <label>Nama Lengkap</label>
                            <span class="value">@Model.ApplicantName</span>
                        </div>
                        <div class="info-item">
                            <label>Email</label>
                            <span class="value">@Model.ApplicantEmail</span>
                        </div>
                        <div class="info-item">
                            <label>Nomor Telepon</label>
                            <span class="value">@Model.ApplicantPhone</span>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Informasi Perusahaan -->
        <div class="info-card full-width">
            <div class="card-header">
                <h3>
                    <i class="fas fa-building"></i>
                    Informasi Perusahaan
                </h3>
            </div>
            <div class="card-body">
                <div class="info-grid">
                    <div class="info-item">
                        <label>Nama Perusahaan</label>
                        <span class="value">@Model.CompanyName</span>
                    </div>
                    <div class="info-item full-width">
                        <label>Alamat Perusahaan</label>
                        <span class="value">@Model.CompanyAddress</span>
                    </div>
                </div>
            </div>
        </div>

        <!-- Detail Pengiriman -->
        <div class="info-card full-width">
            <div class="card-header">
                <h3>
                    <i class="fas fa-shipping-fast"></i>
                    Detail Pengiriman
                </h3>
            </div>
            <div class="card-body">
                <div class="shipping-route">
                    <div class="route-item">
                        <div class="route-icon origin">
                            <i class="fas fa-map-marker-alt"></i>
                        </div>
                        <div class="route-content">
                            <h4>Asal Ternak</h4>
                            <p>@Model.OriginLocation</p>
                            <small>Pelabuhan: @Model.DeparturePort</small>
                        </div>
                    </div>

                    <div class="route-arrow">
                        <i class="fas fa-arrow-right"></i>
                    </div>

                    <div class="route-item">
                        <div class="route-icon destination">
                            <i class="fas fa-map-marker-alt"></i>
                        </div>
                        <div class="route-content">
                            <h4>Tujuan Pengiriman</h4>
                            <p>@Model.DestinationLocation</p>
                            <small>Pelabuhan: @Model.ArrivalPort</small>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Detail Ternak -->
        <div class="info-card full-width">
            <div class="card-header">
                <h3>
                    <i class="fas fa-paw"></i>
                    Detail Ternak
                </h3>
            </div>
            <div class="card-body">
                <div class="livestock-table-container">
                    <table class="livestock-table">
                        <thead>
                            <tr>
                                <th>No.</th>
                                <th>Jenis Ternak</th>
                                <th>Jumlah</th>
                                <th>Keterangan</th>
                            </tr>
                        </thead>
                        <tbody>
                            @for (int i = 0; i < Model.LivestockDetails.Count; i++)
                            {
                                <tr>
                                    <td>@(i + 1)</td>
                                    <td>@Model.LivestockDetails[i].LivestockType</td>
                                    <td>@Model.LivestockDetails[i].Quantity ekor</td>
                                    <td>@(Model.LivestockDetails[i].Description ?? "-")</td>
                                </tr>
                            }
                        </tbody>
                        <tfoot>
                            <tr class="total-row">
                                <td colspan="2"><strong>Total</strong></td>
                                <td><strong>@Model.LivestockDetails.Sum(l => l.Quantity) ekor</strong></td>
                                <td></td>
                            </tr>
                        </tfoot>
                    </table>
                </div>
            </div>
        </div>

        <!-- Dokumen Pendukung -->
        <div class="info-card full-width">
        
            <div class="card-header">
                <h3>
                    <i class="fas fa-file-alt"></i>
                    Dokumen Pendukung
                </h3>
                <!-- ⭐ UPDATE LOGIC PENGHITUNGAN -->
                @{
                    var requiredDocs = new Dictionary<string, string>
                                {
                                { "SURAT_PERMOHONAN", "Surat Permohonan" },
                                { "REKOMENDASI_DINAS_PROV", "Rekomendasi Dinas Peternakan Provinsi NTB" },
                                { "REKOMENDASI_DAERAH_TUJUAN", "Rekomendasi Pemasukan Ternak dari Daerah Tujuan" },
                                { "SKKH_KABUPATEN_ASAL", "SKKH dari Kabupaten Asal" },
                                { "SKKH_DINAS_PROVINSI", "SKKH dari Dinas Peternakan Provinsi NTB" },
                                { "SURAT_JALAN_TERNAK", "Surat Keterangan Jalan Ternak/Rekomendasi Asal" },
                                { "HASIL_PEMERIKSAAN_FISIK", "Hasil Pemeriksaan Fisik (Holding Ground)" }
                                };

                    var optionalDocs = new Dictionary<string, string>
                                {
                                { "DOKUMEN_OPSIONAL", "Dokumen Opsional" }
                                };

                    var requiredDocsCount = Model.Documents.Count(d => requiredDocs.ContainsKey(d.DocumentType));
                    var optionalDocsCount = Model.Documents.Count(d => optionalDocs.ContainsKey(d.DocumentType));
                }

                @if (requiredDocsCount >= 7)
                {
                    <span class="document-status complete">
                        <i class="fas fa-check-circle"></i>
                        Lengkap (@requiredDocsCount/7 wajib
                        @if (optionalDocsCount > 0)
                        {
                            <text> + @optionalDocsCount opsional</text>
                        }
                        )
                    </span>
                }
                else
                {
                    <span class="document-status incomplete">
                        <i class="fas fa-exclamation-triangle"></i>
                        Tidak Lengkap (@requiredDocsCount/7 wajib
                        @if (optionalDocsCount > 0)
                        {
                            <text> + @optionalDocsCount opsional</text>
                        }
                        )
                    </span>
                }
            </div>
            <div class="card-body">
                @if (Model.Documents.Any())
                {
                    <div class="documents-grid">
                        @foreach (var doc in Model.Documents)
                        {
                            <div class="document-card">
                                <div class="document-header">
                                    <div class="document-icon">
                                        @switch (doc.FileExtension.ToLower())
                                        {
                                            case ".pdf":
                                                <i class="fas fa-file-pdf text-danger"></i>
                                                break;
                                            case ".jpg":
                                            case ".jpeg":
                                            case ".png":
                                                <i class="fas fa-file-image text-primary"></i>
                                                break;
                                            default:
                                                <i class="fas fa-file text-secondary"></i>
                                                break;
                                        }
                                    </div>
                                    <div class="document-info">
                                        <h4>@doc.DocumentDisplayName</h4>

                                        <!-- ⭐ TAMBAH BADGE TIPE DOKUMEN -->
                                        <div class="document-badges">
                                            @if (optionalDocs.ContainsKey(doc.DocumentType))
                                            {
                                                <span class="doc-badge optional">Opsional</span>
                                            }
                                            else
                                            {
                                                <span class="doc-badge required">Wajib</span>
                                            }
                                        </div>

                                        <!-- ⭐ TAMPILKAN DETAIL DOKUMEN JIKA ADA -->
                                        @if (!string.IsNullOrEmpty(doc.DocumentNumber) || doc.DocumentDate.HasValue)
                                        {
                                            <div class="document-details">
                                                @if (doc.DocumentDate.HasValue)
                                                {
                                                    <div class="detail-item">
                                                        <i class="fas fa-calendar"></i>
                                                        <span>@doc.DocumentDate.Value.ToString("dd/MM/yyyy")</span>
                                                    </div>
                                                }
                                                @if (!string.IsNullOrEmpty(doc.DocumentNumber))
                                                {
                                                    <div class="detail-item">
                                                        <i class="fas fa-hashtag"></i>
                                                        <span>@doc.DocumentNumber</span>
                                                    </div>
                                                }
                                            </div>
                                        }

                                        <div class="document-meta">
                                            <span class="file-size">@doc.FormattedFileSize</span>
                                            <span class="upload-date">@doc.UploadDate.ToString("dd/MM/yyyy HH:mm")</span>
                                            <span class="uploaded-by">@doc.UploadedBy</span>
                                        </div>
                                    </div>
                                </div>

                                <div class="document-actions">
                                    <a href="@Url.Action("DownloadDocument", "Permit", new { id = doc.Id })" 
                                       class="btn btn-sm btn-outline-primary" 
                                       target="_blank">
                                        <i class="fas fa-download"></i>
                                        Download
                                    </a>
                                    
                                    @if (doc.FileExtension.ToLower() == ".pdf")
                                    {
                                        <button type="button" 
                                                class="btn btn-sm btn-outline-secondary"
                                                onclick="previewDocument('@doc.FilePath', '@doc.DocumentDisplayName')">
                                            <i class="fas fa-eye"></i>
                                            Preview
                                        </button>
                                    }
                                    else if (doc.FileExtension.ToLower() == ".jpg" || doc.FileExtension.ToLower() == ".jpeg" || doc.FileExtension.ToLower() == ".png")
                                    {
                                        <button type="button" 
                                                class="btn btn-sm btn-outline-secondary"
                                                onclick="previewImage('@doc.FilePath', '@doc.DocumentDisplayName')">
                                            <i class="fas fa-eye"></i>
                                            Preview
                                        </button>
                                    }
                                </div>
                            </div>
                        }
                    </div>

                    <!-- Document Requirements Checklist -->
                    <div class="document-summary">
                        <h4>
                            <i class="fas fa-clipboard-check"></i>
                            Status Kelengkapan Dokumen
                        </h4>

                        <div class="checklist-summary">
                            <!-- ⭐ DOKUMEN WAJIB -->
                            @foreach (var requiredDoc in requiredDocs)
                            {
                                var hasDoc = Model.Documents.Any(d => d.DocumentType == requiredDoc.Key);
                                <div class="summary-item @(hasDoc ? "completed" : "missing")">
                                    <i class="fas @(hasDoc ? "fa-check-circle" : "fa-times-circle")"></i>
                                    <span class="doc-name">@requiredDoc.Value</span>
                                    <span class="doc-type-badge required">Wajib</span>
                                    @if (hasDoc)
                                    {
                                        var doc = Model.Documents.First(d => d.DocumentType == requiredDoc.Key);
                                        <small class="file-info">(@doc.FormattedFileSize)</small>
                                    }
                                </div>
                            }

                            <!-- ⭐ DOKUMEN OPSIONAL -->
                            @foreach (var optionalDoc in optionalDocs)
                            {
                                var hasDoc = Model.Documents.Any(d => d.DocumentType == optionalDoc.Key);
                                <div class="summary-item @(hasDoc ? "completed" : "optional")">
                                    <i class="fas @(hasDoc ? "fa-check-circle" : "fa-circle")"></i>
                                    <span class="doc-name">@optionalDoc.Value</span>
                                    <span class="doc-type-badge optional">Opsional</span>
                                    @if (hasDoc)
                                    {
                                        var doc = Model.Documents.First(d => d.DocumentType == optionalDoc.Key);
                                        <small class="file-info">(@doc.FormattedFileSize)</small>
                                    }
                                </div>
                            }
                        </div>
                    </div>
                }
                else
                {
                    <div class="no-documents">
                        <i class="fas fa-folder-open"></i>
                        <h4>Tidak Ada Dokumen</h4>
                        <p>Belum ada dokumen pendukung yang diupload untuk permohonan ini</p>
                    </div>
                }
            </div>
        </div>

        <!-- History Persetujuan -->
        <div class="info-card full-width">
            <div class="card-header">
                <h3>
                    <i class="fas fa-history"></i>
                    Riwayat Persetujuan
                </h3>
            </div>
            <div class="card-body">
                <div class="timeline">
                    @foreach (var history in Model.ApprovalHistory)
                    {
                        <div class="timeline-item">
                            <div class="timeline-marker @GetTimelineClass(history.Action)">
                                <i class="@GetTimelineIcon(history.Action)"></i>
                            </div>
                            <div class="timeline-content">
                                <div class="timeline-header">
                                    <h4>@history.Action</h4>
                                    <span class="timeline-date">@history.ActionDate.ToString("dd MMM yyyy HH:mm")</span>
                                </div>
                                <div class="timeline-body">
                                    <p><strong>Oleh:</strong> @history.ActionBy (@history.ActionByRole)</p>
                                    @if (!string.IsNullOrEmpty(history.Comments))
                                    {
                                        <p><strong>Komentar:</strong> @history.Comments</p>
                                    }
                                    <p><strong>Status:</strong> @PermitStatusHelper.GetStatusText(history.FromStatus) → @PermitStatusHelper.GetStatusText(history.ToStatus)</p>
                                </div>
                            </div>
                        </div>
                    }
                </div>
            </div>
        </div>

        @if (!string.IsNullOrEmpty(Model.RejectionReason))
        {
            <div class="info-card full-width rejection-card">
                <div class="card-header">
                    <h3>
                        <i class="fas fa-exclamation-triangle"></i>
                        Alasan Penolakan
                    </h3>
                </div>
                <div class="card-body">
                    <p class="rejection-reason">@Model.RejectionReason</p>
                </div>
            </div>
        }
    </div>
</div>

<!-- Document Preview Modal -->
<div class="modal fade" id="documentPreviewModal" tabindex="-1">
    <div class="modal-dialog modal-xl">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="previewModalTitle">
                    <i class="fas fa-file-pdf text-danger"></i>
                    Preview Dokumen
                </h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body p-0">
                <div class="preview-container">
                    <div class="preview-toolbar">
                        <div class="toolbar-left">
                            <span id="previewDocumentName">Memuat dokumen...</span>
                        </div>
                        <div class="toolbar-right">
                            <button type="button" class="btn btn-sm btn-outline-secondary" onclick="downloadFromPreview()">
                                <i class="fas fa-download"></i>
                                Download
                            </button>
                        </div>
                    </div>
                    <div class="preview-frame-container">
                        <div class="loading-spinner" id="previewLoader">
                            <div class="spinner-border text-primary" role="status">
                                <span class="visually-hidden">Loading...</span>
                            </div>
                            <p>Memuat preview dokumen...</p>
                        </div>
                        <iframe id="previewFrame" style="display: none; width: 100%; height: 600px; border: none;"></iframe>
                        <div class="preview-error" id="previewError" style="display: none;">
                            <i class="fas fa-exclamation-triangle text-warning"></i>
                            <p>Gagal memuat preview dokumen. Silakan download untuk melihat dokumen.</p>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Image Preview Modal -->
<div class="modal fade" id="imagePreviewModal" tabindex="-1">
    <div class="modal-dialog modal-lg">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="imageModalTitle">
                    <i class="fas fa-file-image text-primary"></i>
                    Preview Gambar
                </h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body text-center">
                <img id="previewImage" src="" alt="Preview" class="img-fluid" style="max-height: 600px;" />
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Tutup</button>
                <button type="button" class="btn btn-primary" onclick="downloadFromImagePreview()">
                    <i class="fas fa-download"></i>
                    Download
                </button>
            </div>
        </div>
    </div>
</div>

@functions {
    private string GetTimelineClass(string action)
    {
        return action.ToLower() switch
        {
            "submit" => "primary",
            "disetujui admin" => "success",
            "ditolak admin" => "danger",
            "disetujui verifikator" => "success",
            "ditolak verifikator" => "danger",
            "disetujui kepala dinas" => "success",
            "ditolak kepala dinas" => "danger",
            _ => "secondary"
        };
    }

    private string GetTimelineIcon(string action)
    {
        return action.ToLower() switch
        {
            "submit" => "fas fa-paper-plane",
            "disetujui admin" => "fas fa-check",
            "ditolak admin" => "fas fa-times",
            "disetujui verifikator" => "fas fa-check",
            "ditolak verifikator" => "fas fa-times",
            "disetujui kepala dinas" => "fas fa-stamp",
            "ditolak kepala dinas" => "fas fa-ban",
            _ => "fas fa-circle"
        };
    }
}

@section Scripts {
    <script>
        let currentPreviewPath = '';
        let currentImagePath = '';

        function previewDocument(filePath, documentName) {
            currentPreviewPath = filePath;
            const modal = new bootstrap.Modal(document.getElementById('documentPreviewModal'));
            
            // Reset modal state
            document.getElementById('previewDocumentName').textContent = documentName;
            document.getElementById('previewLoader').style.display = 'flex';
            document.getElementById('previewFrame').style.display = 'none';
            document.getElementById('previewError').style.display = 'none';

            // Load document in iframe
            const iframe = document.getElementById('previewFrame');
            iframe.src = filePath;

            // Handle iframe load
            iframe.onload = function() {
                document.getElementById('previewLoader').style.display = 'none';
                iframe.style.display = 'block';
            };

            iframe.onerror = function() {
                document.getElementById('previewLoader').style.display = 'none';
                document.getElementById('previewError').style.display = 'flex';
            };

            // Fallback timeout
            setTimeout(() => {
                if (document.getElementById('previewLoader').style.display !== 'none') {
                    document.getElementById('previewLoader').style.display = 'none';
                    iframe.style.display = 'block';
                }
            }, 3000);

            modal.show();
        }

        function previewImage(imagePath, imageName) {
            currentImagePath = imagePath;
            const modal = new bootstrap.Modal(document.getElementById('imagePreviewModal'));
            
            document.getElementById('imageModalTitle').innerHTML = `<i class="fas fa-file-image text-primary"></i> ${imageName}`;
            document.getElementById('previewImage').src = imagePath;

            modal.show();
        }

        function downloadFromPreview() {
            if (currentPreviewPath) {
                window.open(currentPreviewPath, '_blank');
            }
        }

        function downloadFromImagePreview() {
            if (currentImagePath) {
                window.open(currentImagePath, '_blank');
            }
        }

        // Clean up when modals are closed
        document.getElementById('documentPreviewModal').addEventListener('hidden.bs.modal', function() {
            const iframe = document.getElementById('previewFrame');
            iframe.src = 'about:blank';
            currentPreviewPath = '';
        });

        document.getElementById('imagePreviewModal').addEventListener('hidden.bs.modal', function() {
            document.getElementById('previewImage').src = '';
            currentImagePath = '';
        });
    </script>
}

<style>
    /* Document Display Styles */
    .documents-grid {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
        gap: 20px;
        margin-bottom: 30px;
    }

    .document-card {
        border: 2px solid #e9ecef;
        border-radius: 8px;
        padding: 15px;
        transition: all 0.3s;
        background: #f8f9fa;
    }

    .document-card:hover {
        border-color: #667eea;
        transform: translateY(-2px);
    }

    .document-header {
        display: flex;
        align-items: flex-start;
        gap: 15px;
        margin-bottom: 15px;
    }

    .document-icon {
        font-size: 32px;
        flex-shrink: 0;
        margin-top: 5px;
    }

    .document-info {
        flex: 1;
        min-width: 0;
    }

    .document-info h4 {
        margin: 0 0 8px 0;
        color: #333;
        font-size: 14px;
        font-weight: 600;
        line-height: 1.3;
    }

    .document-meta {
        display: flex;
        flex-direction: column;
        gap: 4px;
        font-size: 12px;
        color: #666;
    }

    .document-meta span {
        display: block;
    }

    .document-actions {
        display: flex;
        gap: 8px;
        flex-wrap: wrap;
    }

    .document-actions .btn {
        font-size: 12px;
        padding: 6px 12px;
    }

    .document-summary {
        background: #f8f9fa;
        border: 2px solid #e9ecef;
        border-radius: 8px;
        padding: 20px;
        margin-top: 20px;
    }

    .document-summary h4 {
        margin: 0 0 15px 0;
        color: #333;
        font-size: 16px;
        font-weight: 600;
        display: flex;
        align-items: center;
        gap: 10px;
    }

    .document-summary h4 i {
        color: #667eea;
    }

    .checklist-summary {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
        gap: 10px;
    }

    .summary-item {
        display: flex;
        align-items: center;
        gap: 10px;
        padding: 8px 12px;
        border-radius: 6px;
        font-size: 14px;
        font-weight: 500;
    }

    .summary-item.completed {
        background: #d4edda;
        color: #155724;
    }

    .summary-item.completed i {
        color: #28a745;
    }

    .summary-item.missing {
        background: #f8d7da;
        color: #721c24;
    }

    .summary-item.missing i {
        color: #dc3545;
    }

    .file-info {
        font-size: 11px;
        opacity: 0.8;
        margin-left: auto;
    }

    .document-status {
        padding: 6px 12px;
        border-radius: 20px;
        font-size: 12px;
        font-weight: 600;
        display: flex;
        align-items: center;
        gap: 5px;
    }

    .document-status.complete {
        background: #d4edda;
        color: #155724;
    }

    .document-status.incomplete {
        background: #fff3cd;
        color: #856404;
    }

    .no-documents {
        text-align: center;
        padding: 40px;
        color: #666;
    }

    .no-documents i {
        font-size: 48px;
        color: #e9ecef;
        margin-bottom: 15px;
    }

    .no-documents h4 {
        margin: 0 0 10px 0;
        color: #333;
    }

    .no-documents p {
        margin: 0;
    }

    /* Preview Modal Styles */
    .preview-container {
        width: 100%;
    }

    .preview-toolbar {
        background: #f8f9fa;
        padding: 15px 20px;
        border-bottom: 1px solid #e9ecef;
        display: flex;
        justify-content: space-between;
        align-items: center;
    }

    .toolbar-left {
        font-weight: 600;
        color: #333;
    }

    .toolbar-right {
        display: flex;
        gap: 10px;
    }

    .preview-frame-container {
        position: relative;
        background: #f8f9fa;
        min-height: 600px;
    }

    .loading-spinner, .preview-error {
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        height: 400px;
        color: #666;
    }

    .loading-spinner p, .preview-error p {
        margin-top: 15px;
        margin-bottom: 0;
    }

    .preview-error i {
        font-size: 48px;
        margin-bottom: 15px;
    }

    /* Existing styles from original Detail.cshtml */
    .permit-detail-container {
        padding: 20px;
        max-width: 1200px;
        margin: 0 auto;
    }

    .detail-header {
        display: flex;
        justify-content: space-between;
        align-items: flex-start;
        margin-bottom: 30px;
        padding-bottom: 20px;
        border-bottom: 2px solid #e9ecef;
    }

    .header-content {
        display: flex;
        justify-content: space-between;
        align-items: center;
        width: 100%;
    }

    .detail-title {
        color: #333;
        margin: 0;
        font-size: 28px;
        font-weight: 600;
    }

    .detail-title i {
        margin-right: 12px;
        color: #667eea;
    }

    .header-actions {
        display: flex;
        gap: 10px;
    }

    .permit-status {
        margin-top: 10px;
    }

    .status-badge {
        padding: 8px 16px;
        border-radius: 20px;
        font-size: 14px;
        font-weight: 600;
        text-transform: uppercase;
    }

    .badge-secondary {
        background: #6c757d;
        color: white;
    }

    .badge-info {
        background: #17a2b8;
        color: white;
    }

    .badge-warning {
        background: #ffc107;
        color: #212529;
    }

    .badge-primary {
        background: #007bff;
        color: white;
    }

    .badge-danger {
        background: #dc3545;
        color: white;
    }

    .badge-success {
        background: #28a745;
        color: white;
    }

    .detail-content {
        display: flex;
        flex-direction: column;
        gap: 25px;
    }

    .content-grid {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 25px;
    }

    .info-card {
        background: white;
        border-radius: 12px;
        box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
        overflow: hidden;
    }

    .info-card.full-width {
        grid-column: 1 / -1;
    }

    .card-header {
        background: #f8f9fa;
        padding: 20px;
        border-bottom: 1px solid #e9ecef;
        display: flex;
        justify-content: space-between;
        align-items: center;
    }

    .card-header h3 {
        margin: 0;
        color: #333;
        font-size: 18px;
        font-weight: 600;
    }

    .card-header i {
        margin-right: 10px;
        color: #667eea;
    }

    .card-body {
        padding: 25px;
    }

    .info-grid {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 20px;
    }

    .info-item {
        display: flex;
        flex-direction: column;
    }

    .info-item.full-width {
        grid-column: 1 / -1;
    }

    .info-item label {
        font-size: 14px;
        color: #666;
        margin-bottom: 5px;
        font-weight: 500;
    }

    .info-item .value {
        font-size: 16px;
        color: #333;
        font-weight: 600;
    }

    .info-item .value.highlight {
        color: #667eea;
        font-size: 18px;
    }

    .shipping-route {
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 30px;
    }

    .route-item {
        display: flex;
        align-items: center;
        flex: 1;
        gap: 15px;
    }

    .route-icon {
        width: 50px;
        height: 50px;
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 20px;
        color: white;
    }

    .route-icon.origin {
        background: #28a745;
    }

    .route-icon.destination {
        background: #dc3545;
    }

    .route-content h4 {
        margin: 0 0 5px 0;
        color: #333;
        font-size: 16px;
        font-weight: 600;
    }

    .route-content p {
        margin: 0 0 5px 0;
        color: #333;
        font-size: 14px;
    }

    .route-content small {
        color: #666;
        font-size: 12px;
    }

    .route-arrow {
        font-size: 24px;
        color: #667eea;
        margin: 0 20px;
    }

    .livestock-table-container {
        overflow-x: auto;
    }

    .livestock-table {
        width: 100%;
        border-collapse: collapse;
        margin: 0;
    }

    .livestock-table th,
    .livestock-table td {
        padding: 12px;
        text-align: left;
        border-bottom: 1px solid #e9ecef;
    }

    .livestock-table th {
        background: #f8f9fa;
        font-weight: 600;
        color: #333;
    }

    .livestock-table tbody tr:hover {
        background: #f8f9fa;
    }

    .total-row {
        background: #f8f9fa;
        font-weight: 600;
    }

    .timeline {
        position: relative;
    }

    .timeline::before {
        content: '';
        position: absolute;
        left: 25px;
        top: 0;
        bottom: 0;
        width: 2px;
        background: #e9ecef;
    }

    .timeline-item {
        position: relative;
        display: flex;
        margin-bottom: 30px;
    }

    .timeline-marker {
        width: 50px;
        height: 50px;
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        color: white;
        font-size: 16px;
        margin-right: 20px;
        position: relative;
        z-index: 2;
    }

    .timeline-marker.primary {
        background: #007bff;
    }

    .timeline-marker.success {
        background: #28a745;
    }

    .timeline-marker.danger {
        background: #dc3545;
    }

    .timeline-marker.secondary {
        background: #6c757d;
    }

    .timeline-content {
        flex: 1;
        background: #f8f9fa;
        border-radius: 8px;
        padding: 20px;
        margin-top: 5px;
    }

    .timeline-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 10px;
    }

    .timeline-header h4 {
        margin: 0;
        color: #333;
        font-size: 16px;
        font-weight: 600;
    }

    .timeline-date {
        color: #666;
        font-size: 12px;
    }

    .timeline-body p {
        margin: 5px 0;
        color: #333;
        font-size: 14px;
    }

    .rejection-card {
        border-left: 4px solid #dc3545;
    }

    .rejection-reason {
        background: #fff5f5;
        padding: 15px;
        border-radius: 8px;
        color: #721c24;
        font-size: 14px;
        margin: 0;
    }

    @@media (max-width: 768px) {
        .permit-detail-container {
            padding: 15px;
        }

        .header-content {
            flex-direction: column;
            align-items: flex-start;
            gap: 15px;
        }

        .header-actions {
            width: 100%;
            justify-content: flex-start;
        }

        .content-grid {
            grid-template-columns: 1fr;
        }

        .info-grid {
            grid-template-columns: 1fr;
        }

        .shipping-route {
            flex-direction: column;
            gap: 20px;
        }

        .route-arrow {
            transform: rotate(90deg);
            margin: 10px 0;
        }

        .detail-title {
            font-size: 24px;
        }

        .timeline::before {
            left: 20px;
        }

        .timeline-marker {
            width: 40px;
            height: 40px;
            font-size: 14px;
        }

        .documents-grid {
            grid-template-columns: 1fr;
        }

        .checklist-summary {
            grid-template-columns: 1fr;
        }

        .document-actions {
            flex-direction: column;
        }

        .document-actions .btn {
            width: 100%;
        }
    }
    /* ⭐ TAMBAHAN CSS UNTUK DOKUMEN OPSIONAL */
    .document-card.optional-doc {
        border-color: #17a2b8;
        background: #f0fcff;
    }

    .document-card.required-doc {
        border-color: #e9ecef;
        background: #f8f9fa;
    }

    .document-badges {
        margin-bottom: 8px;
    }

    .doc-badge {
        display: inline-block;
        padding: 2px 8px;
        border-radius: 12px;
        font-size: 10px;
        font-weight: 600;
        text-transform: uppercase;
    }

        .doc-badge.required {
            background: #fff3cd;
            color: #856404;
        }

        .doc-badge.optional {
            background: #d1ecf1;
            color: #0c5460;
        }

    .document-details {
        margin-bottom: 8px;
        display: flex;
        flex-wrap: wrap;
        gap: 10px;
    }

    .detail-item {
        display: flex;
        align-items: center;
        gap: 4px;
        font-size: 11px;
        color: #666;
        background: rgba(255,255,255,0.7);
        padding: 2px 6px;
        border-radius: 4px;
    }

        .detail-item i {
            font-size: 10px;
        }

    .summary-item.optional {
        background: #d1ecf1;
        color: #0c5460;
    }

        .summary-item.optional i {
            color: #17a2b8;
        }

    .summary-item {
        display: flex;
        align-items: center;
        gap: 10px;
        padding: 8px 12px;
        border-radius: 6px;
        font-size: 14px;
        font-weight: 500;
        position: relative;
    }

    .doc-name {
        flex: 1;
    }

    .doc-type-badge {
        padding: 2px 6px;
        border-radius: 8px;
        font-size: 10px;
        font-weight: 600;
        text-transform: uppercase;
    }

        .doc-type-badge.required {
            background: rgba(255, 243, 205, 0.8);
            color: #856404;
        }

        .doc-type-badge.optional {
            background: rgba(209, 236, 241, 0.8);
            color: #0c5460;
        }
</style>